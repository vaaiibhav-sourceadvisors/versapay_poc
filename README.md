# versapay_poc
versapay implementation testing


#Running backend

cd ./backend

pip install fastapi uvicorn httpx python-dotenv

uvicorn main:app --reload


#Running Frontend

open debug.html in the live_server(extension can be found on VS code)
